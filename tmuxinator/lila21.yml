project_name: lila21
project_root: ~/dev/lila21
pre: sudo systemctl start mongodb.service && sudo -u elasticsearch /usr/share/elasticsearch/bin/elasticsearch
tabs:
  - main:
      layout: even-horizontal
      panes:
        - cd ~/dev/lila21 && vim
        - cd ~/dev/lila21 && ./bin/play
  - shell: cd ~/dev/lila21
